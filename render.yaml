services:
  - type: web
    name: antigravity-video-downloader
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_PASSWORD
        generateValue: true
